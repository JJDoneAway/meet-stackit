management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: prometheus,info,health
  metrics:
    tags:
      application: meet-stackit
server:
  tomcat:
    accesslog:
      buffered: false
      directory: /dev
      enabled: true
      file-date-format: ''
      prefix: stdout
      suffix: ''

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: a9sd35b6f3c0a2ed61f1f4feb4222d87283a4dd769a
    url: jdbc:mysql://${MYSQL_HOST:msd38a3ec-mysql-0-0.data.eu01.onstackit.cloud}:49273/msd38a3ec
    username: ${DB_USERNAME}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
